// TrouveTonPro/index.js (L'ancien App.js)

import { registerRootComponent } from 'expo';
import App from './App'; // Importe le composant App qui se trouve dans App.js

// Ceci est la ligne qui dit à Expo de démarrer l'application avec le composant App.
registerRootComponent(App);